part of '../{{feature_name}}_page.dart';

class _{{feature_name.pascalCase()}}Screen extends StatelessWidget {
  const _{{feature_name.pascalCase()}}Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}