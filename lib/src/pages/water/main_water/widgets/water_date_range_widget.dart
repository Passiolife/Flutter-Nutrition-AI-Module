import 'package:flutter/material.dart';

class WaterDateRangeWidget extends StatelessWidget {
  const WaterDateRangeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
